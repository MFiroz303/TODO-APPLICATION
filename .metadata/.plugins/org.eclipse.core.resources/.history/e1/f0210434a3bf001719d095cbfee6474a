var todoApp = angular.module("todoApp");
todoApp.factory('homeService', function($http, $location){
	
	var home = {};
	 
	home.cardUser = function(note) {
		return $http({
			method : "POST",
			url : 'list',
			data : note

		/*}).then(function(response) {
			$location.path('/home')*/
		});
	}
	return login;
});

