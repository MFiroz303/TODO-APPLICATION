var todoApp = angular.module('todoApp');
todoApp.config('registerService'function($http, $location))

   registerUser = function(user){
   
   $http({
	      method = "POST",
	      url= 'register',
	      data = user

         });
   }
